<%= form_for(@output) do |f| %>


  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad , :step => 'any' %>
  </div>
  
  <div class="field">
    <label>Artículo</label>
  </div>
  <div class="field">
        <li>
          <%= radio_button_tag "output[article_id]", @article.id, :checked => true %> <%= @article.nombre %>
        </li>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
