<p id="notice"><%= notice %></p>

<ul class="no-list row">
      <li class="col-md">
            <h2>
              <strong>Nombre:</strong>
              <%= @article.nombre %>
            </h2>

          <p>
            <strong>Desc:</strong>
            <%= @article.desc %>
          </p>
          <p>
            <strong>Último pedido:</strong>
            <% if @article.ultimoped.present? %>
              <%= @article.ultimoped %>
            <% end  %>

          </p>

          <p>
            <strong>Saldo:</strong>
            <%= @article.saldo.round(2) %>
          </p>

          <p>
            <strong>Stockmin:</strong>
            <%= @article.stockmin %>
          </p>

          <p>
            <strong>Lotecompra:</strong>
            <%= @article.lotecompra %>
          </p>

          <p>
            <strong>Unidad:</strong>
            <%= @article.unidad %>
          </p>

          <p>
            <strong>Unidadcompra:</strong>
            <%= @article.unidadcompra %>
          </p>

          <p>
            <strong>Ucompraporudeuso:</strong>
            <%= @article.ucompraporudeuso %>
          </p>

          <p>
            <strong>Sfamily:</strong>
            <%= @article.sfamily %>
          </p>

          <p>
            <strong>Family:</strong>
            <%= @article.family %>
          </p>

          <p>
            <strong>Ssfamily:</strong>
            <%= @article.ssfamily %>
          </p>
      </li>
      <li class="col-md">
        <h3> Saldo </h3>
        <div>
          <% @balances.reverse.each do |balance| %>
            <div>
              <%= link_to balance.saldo.round(2), balance %> -  <%=balance.updated_at%>
            </div>
          <%end%>
        </div>
      </li>
      <li class="col-md">
        <h3> Entradas </h3>
        <div>
          <% @inputs.reverse.each do |input| %>
            <div>
              <%= link_to input.cantidad.round(2), input %> -  <%=input.updated_at%>
            </div>
          <% end %>
        </div>
      </li>
      <li class="col-md">
        <h3> Salidas </h3>
        <div>
          <% @outputs.reverse.each do |output| %>
            <div>
              <%= link_to output.cantidad.round(2), output %> -  <%=output.updated_at%>
            </div>
          <%end%>
        </div>
      </li>
    </ul>

<%= link_to 'Back', articles_path %>
