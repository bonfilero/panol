<%= form_for(@sfamily) do |f| %>
  <% if @sfamily.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sfamily.errors.count, "error") %> prohibited this sfamily from being saved:</h2>

      <ul>
      <% @sfamily.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="field">
    <%= f.label :family_id %><br>
    <%= f.text_field :family_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
