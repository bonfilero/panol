<%= form_for(@ssystem) do |f| %>
  <% if @ssystem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ssystem.errors.count, "error") %> prohibited this ssystem from being saved:</h2>

      <ul>
      <% @ssystem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :system_id %><br>
    <%= f.text_field :system_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
